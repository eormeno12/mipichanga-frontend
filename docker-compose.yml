version: '3.3'

services:
  mipichanga-frontend:
    image: mipichanga-frontend
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    extra_hosts:
      - "host.docker.internal:host-gateway"